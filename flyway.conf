# Flyway Maven 插件配置
# 运行 mvn flyway:repair / migrate / info 等命令时会自动读取此文件
# 也可以通过环境变量覆盖：FLYWAY_URL、FLYWAY_USER、FLYWAY_PASSWORD

# 与 application-dev.yml 中的数据源保持一致
flyway.url=jdbc:mysql://localhost:3306/scheduled_task?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
flyway.user=root
flyway.password=Yanping69!

flyway.locations=classpath:db/migration
flyway.baselineOnMigrate=true
flyway.validateOnMigrate=true
flyway.placeholderReplacement=false
