spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB:scheduled_task}?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:Yanping69!}

report:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:1236}

logging:
  level:
    com.mattoid.scheduled: debug
