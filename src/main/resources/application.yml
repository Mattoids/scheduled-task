server:
  port: 1236

spring:
  application:
    name: scheduled-task
  profiles:
    active: dev
  sql:
    init:
      mode: never
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      column-format: "`%s`"

report:
  jwt:
    secret: ${JWT_SECRET:scheduled-task-secret-key-change-in-production}
    expiration: 86400000
  upload:
    path: ${user.home}/scheduled-task/uploads
  default:
    page-size: 20
